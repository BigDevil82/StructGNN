当前项目是我当前研究课题的代码库，我做的是剪力墙结构智能设计研究，通过训练AI模型来预测给定建筑布局下剪力墙的布置方案，也就是确定哪些位置需要布置剪力墙，目前仅涉及剪力墙位置预测，不涉及构件尺寸，且完全是数据驱动方式。

下面梳理一下当前代码库的内容，以便后期更好维护和管理。以下将该研究中剪力墙方案预测模型称为“RC-GNN (Room-level Conditional GNN)”

## 核心代码说明

当前项目主要包含数据集的前处理，模型训练及推理，有限元建模分析代码4个主要部分。

1. 数据集处理

由于所需的建筑图纸难以收集，因此采用了开源的一些图像数据集，它们是由cad图纸提取转换而来，通过不同颜色将几类关键构件绘制为粗线条（墙体、门、窗）。剪力墙布局生成模型的输入是建筑图，输出是结构图，建筑图中包含填充墙、门、窗三类构件，结构图中包含剪力墙、填充墙、门、窗4类构件（也就是将建筑图中一些位置的填充墙替换为了剪力墙）。

由于能拿到的是图片数据，但我想做的是基于GNN的矢量预测，而不是图生图，因此需要先将图片转换为矢量格式，更具体来说就是从图片中提取出代表每段构件的中轴线。

图片处理部分的代码在`pngtool`文件夹中，我先通过`image_clustering`将数据集中很多重复的图片去重，然后再通过`png2dxf_converter`提取各类构件的外轮廓，并绘制在dxf中，这样方便进行后续标注。既有研究都是以构件为基本单元构件图（Graph），然后预测每个构件两端剪力墙率，但是我认为将整个布局拆分为以墙段为基本单元会丧失一些空间上的语义信息，所以我将房间作为基本单元，因为住宅建筑基本都有房间分区，更符合工程直觉。因此我们在转换后的dxf中手动标注了每张图纸的所有房间，对于不规则区域强行通过矩形分割。

有了上述标注好的图纸，就可以进行数据集前处理，将其转换为模型训练所需的格式了。这部分代码在`preprocess`文件夹中。`dxf_extractor`用于从dxf图纸中提取各个图层构件，每个构件的外轮廓坐标构成的一个多段线列表。`room_calibrator`用于对标注的房间进行校准，确保临近的房间边界是严格重合的，消除绘制误差。`room_analyzer`则是将房间的四边与其余构件做几何运算，确定哪些位置包含剪力墙，从而确定各条边上剪力墙的分布比例，用作数据标签。然后再通过`layout_graph`将一张平面布局转换为图结构，以房间为节点，房间之间如果有重叠边，则连接一条边。节点和边包含了房间一些几何属性特征。

2. 模型训练

数据集准备好之后，模型训练相关代码在 `shearwall_pred`文件夹中，其中包含了训练配置、数据增广、数据集定义及划分、模型架构、损失函数、训练器、交叉验证、测试集预测可视化等相关模块，代码划分相对比较清晰。

3. 模型推理

推理阶段的输入和训练时不太一样，因为实际应用的时候不再是图片了，而是原始的cad图纸，这个时候需要从cad图纸中提取到所需构件信息并转换为训练类似的输入。cad前处理这一块开发了相应的插件，导出json格式的墙、门、窗数据。为了将双线表示的构件转换为中轴线的一维表示，在`axis_engine`文件夹中实现了相关处理算法：`wall_centerline` 用于提取轴线，`line_network_calibrator`用于对提取后的轴线进行校准，确保连接准确。得到一维的墙体、门窗表示后，用`rect_decomposer`将整个布局分割为矩形，从而得到模型所需要的“房间节点”，最后在`cad_processor`中将上述流程组合在一起。

为了无缝对接现有的代码，`preprocess\cad_inference.py`将上述处理后的结果写入dxf文件，从而可直接作为模型推理时的输入。

模型推理的接口在`experiments\case_study\run_case_study.py`中，原本`experiments`文件夹是存放论文中各种实验相关的代码，包括消融实验、指标计算、绘图、案例分析等等，后面由于逐步开发过程中`case_study`的相关代码越来越完备，就在这个文件夹中存放了一些使用较为频繁的代码，其实应该抽离到一个顶层文件夹。


4. 建模分析

上面仅完成了剪力墙预测，但是结构方案是否可行还需要进行最终的力学分析校核，因此需要将预测结果解析为可用于FEM建模的构件信息，这部分相关代码主要在`experiments\case_study`文件夹中。`fem_builder`是用于将预测结果转换为可建模的力学构件建模信息，主要包含剪力墙、梁两种构件的坐标信息。为了确保对称布局输入下得到的剪力墙布置也是对称的，`symmetry_postprocess`对生成结果进行了对称化处理。`create_etabs_model`是根据解析结果建立etabs模型的代码。其中还有一个子文件夹`yjk_pipeline`则是调用盈建科api进行建模分析。


5. 后续研究

由于上述研究仅仅得到剪力墙布局，且纯数据驱动，不涉及力学信息，为了让方案符合工程实践，还需要对构件尺寸进行智能设计，目前大致计划通过优化来对初始的默认构件（墙厚、梁尺寸）进行截面优化，从而得到满足力学要求且节约材料的结构方案。为了能快速评估某个特定结构的力学指标，需要一个快速建模以及快速分析方案，计划采用opensees进行建模分析，相关代码存放在`shearwall_modeling`文件夹中。

为了得到大量结构模型，再次充分利用开源数据集中的结构方案，`convert_dxf`将其转换为可建模的json格式文件；`shearwall_modeling\builders\detailed_shell.py`则是一个精细的剪力墙结构建模方案，`evaluation`根据建模结果进行模态、反应谱分析得到一些整体指标，并对照规范进行校核。

## 补充说明

以上就是当前项目的主要代码，剩余的一些次要文件夹说明如下：

- %TEMP%: 存放了opensees源码，便于编码智能体查看
- baseline_edge_gnn：根据既有研究论文中的介绍复现的，以墙段为基本图元的剪力墙预测模型相关训练代码
- beam_pred：原本想做梁的预测模型，但是发现现有这种实现无需额外训练一个梁预测模型，因为剪力墙预测完成后只需将房间剩余区域通过梁连接即可。
- data_cache：训练模型时pyG的数据缓存目录
- dxf：各种原始数据
- paper: 论文相关
- result： 存放各种结果
- experiments：除case_study外，其余均是写论文时的临时代码，无需长期使用和维护
- misc: 一些辅助功能函数。

ps: 原本这个项目只是用于将原始png数据集转换为dxf，后来随着开发的深入，各种功能都放在了这里，但是也不便再改名了，是一个小问题。