\section{Experimental Results and Discussions}
\label{sec:results_discussion}

This section presents the experimental results and discusses the effectiveness of the proposed calibrated surrogate-assisted optimization framework. 

\subsection{Global Surrogate Model Performance}
\label{subsec:global_surrogate_results}

Fig.~\ref{fig:global_surrogate_overview} shows that the global LayoutParamGNN provides strong feasibility separation and reinforcement ranking ability on unseen layouts, but it also exposes two important limitations: layout-dependent probability bias and systematic underestimation in high-reinforcement regions. The precision--recall curve in Fig.~\ref{fig:global_surrogate_overview}(a) supports the feasibility-screening capability, whereas the layout-wise probability comparison in Fig.~\ref{fig:global_surrogate_overview}(b) and the reinforcement error distribution in Fig.~\ref{fig:global_surrogate_overview}(d) reveal the need for local calibration. Tables~\ref{tab:global_feasibility_results} and~\ref{tab:global_steel_results} further compare the proposed graph-based surrogate with two reduced input settings to examine the contribution of layout information.


\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{optimization_benchmark_results.pdf}
\caption{Optimization efficiency and solution quality under different optimizers and candidate evaluation strategies.}
\label{fig:optimization_benchmark_results}
\end{figure*}



\begin{table*}[t]
\centering
\caption{Optimization efficiency and solution quality under different candidate evaluation strategies.}
\label{tab:optimization_summary_results}
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.13\textwidth}
>{\raggedright\arraybackslash}p{0.13\textwidth}
>{\centering\arraybackslash}p{0.055\textwidth}
>{\centering\arraybackslash}p{0.095\textwidth}
>{\centering\arraybackslash}X
>{\centering\arraybackslash}p{0.105\textwidth}
>{\centering\arraybackslash}X
>{\centering\arraybackslash}p{0.12\textwidth}@{}}
\toprule
Algorithm & Method & Runs & Success rate & Mean FEA calls & FEA reduction & First feasible FEA & Cost ratio vs. full \\
\midrule
\multirow{3}{*}{GA}
& Full FEA & 75 & 68.0\% & 650 & -- & 72 & -- \\
& Cost ranking & 75 & 60.0\% & 279 & 57.0\% & 24 & 1.016 \\
& Screen \& ranking & 75 & 65.3\% & 223 & 65.7\% & 28 & 1.018 \\
\midrule
\multirow{3}{*}{PSO}
& Full FEA & 75 & 61.3\% & 573 & -- & 105 & -- \\
& Cost ranking & 75 & 57.3\% & 261 & 54.4\% & 56 & 1.006 \\
& Screen \& ranking & 75 & 58.7\% & 215 & 62.4\% & 51 & 1.017 \\
\midrule
\multirow{3}{*}{Random search}
& Full FEA & 75 & 68.0\% & 717 & -- & 71 & -- \\
& Cost ranking & 75 & 66.7\% & 358 & 50.1\% & 42 & 1.000 \\
& Screen \& ranking & 75 & 66.7\% & 247 & 65.6\% & 31 & 1.002 \\
\bottomrule
\end{tabularx}
\end{table*}



\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{cost_tolerance_analysis.pdf}
\caption{Cost-tolerance analysis of surrogate-assisted strategies in full-feasible cases.}
\label{fig:cost_tolerance_analysis}
\end{figure*}


\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth,height=0.25\textheight,keepaspectratio]{candidate_flow_analysis.pdf}
\caption{Candidate flow in the calibrated surrogate-assisted optimization process.}
\label{fig:candidate_flow_analysis}
\end{figure*}

\begin{figure*}[!t]
\centering
\includegraphics[width=\textwidth]{optimization_case_examples.pdf}
\caption{Representative optimization histories for layouts with different levels of difficulty.}
\label{fig:optimization_case_examples}

\vspace{0.4em}

\includegraphics[width=\textwidth]{layout_difficulty_heatmap.pdf}
\caption{Feasible-solution discovery patterns across test layouts and optimization strategies.}
\label{fig:layout_difficulty_heatmap}
\end{figure*}

Table~\ref{tab:global_feasibility_results} compares the feasibility surrogate under three input settings. When only design parameters are used, the model achieves a PR-AUC of 0.6137 and an F1 score of 0.5927 on the test layouts. Adding global layout statistics improves the PR-AUC to 0.8163 and the F1 score to 0.7370, indicating that layout-level geometric and topological descriptors provide important information for feasibility judgment. The proposed LayoutParamGNN further improves the ROC-AUC and PR-AUC to 0.9693 and 0.8593, respectively, with an F1 score of 0.7688. The corresponding precision--recall curve in Fig.~\ref{fig:global_surrogate_overview}(a) also shows that the graph-based feasibility model separates feasible and infeasible candidates substantially better than the baseline feasible rate. This improvement suggests that the room-level graph representation captures additional spatial organization information that cannot be fully represented by manually defined layout statistics alone.


From the perspective of optimization, the feasibility surrogate mainly supports conservative screening. Under the selected screening threshold, the LayoutParamGNN rejects approximately 47.36\% of candidate samples while maintaining a feasible recall of 0.9998, with only 3 truly feasible samples being falsely rejected. In comparison, the design-parameter-only model produces more false rejections under a similar reject rate. The layout-statistics model reduces the false reject count to 10, but its overall PR-AUC and F1 score remain lower than those of the LayoutParamGNN. These results support the use of the room-level graph model as the global feasibility surrogate in the subsequent optimization experiments.

The reinforcement usage prediction results are shown in Table~\ref{tab:global_steel_results}. The design-parameter-only MLP obtains an MAE of 32.30~t and an $R^2$ of 0.351, which is insufficient for reliable candidate ranking. After adding global layout statistics, the MAE decreases to 13.83~t and the $R^2$ increases to 0.891. The LayoutParamGNN further reduces the MAE to 11.48~t and improves the $R^2$ to 0.924. The consistent improvement in both feasibility prediction and reinforcement estimation confirms that shear wall structural performance and reinforcement demand are strongly affected by layout characteristics.


Since the reinforcement surrogate is mainly used to rank candidates before FEA, ranking quality is also important. The LayoutParamGNN achieves a pairwise ranking accuracy of 0.9787, a Spearman correlation of 0.9779, and a top-10\% recall of 0.9483. These results indicate that the model can reliably compare the relative reinforcement demand of different candidates within the same layout, even if the exact reinforcement quantity still contains prediction error.

The same figure also explains why a static global surrogate is not sufficient for direct optimization. In Fig.~\ref{fig:global_surrogate_overview}(b), several layouts with low true feasible rates still receive over-optimistic feasibility probabilities. In Fig.~\ref{fig:global_surrogate_overview}(d), the reinforcement error becomes increasingly biased for high-demand candidates; in the highest 20\% reinforcement-usage interval, the MAE increases to approximately 20.7~t and the bias reaches about $-17.4$~t. These layout- and demand-dependent errors motivate the online local calibration strategy analyzed in the next subsection.

\subsection{Effectiveness of Online Local Calibration}
\label{subsec:local_calibration_results}

Fig.~\ref{fig:local_calibration_effect} shows that 100 FEA-verified samples are sufficient to improve surrogate reliability for most test layouts. The paired layout-wise comparisons in Fig.~\ref{fig:local_calibration_effect}(a) and Fig.~\ref{fig:local_calibration_effect}(b) indicate a consistent leftward shift after calibration, meaning lower Brier scores for feasibility prediction and lower MAE for reinforcement estimation. The largest gains occur on layouts where the global surrogate has larger original errors, which supports the role of local calibration as a bias-correction mechanism rather than a replacement for the global model.

Table~\ref{tab:local_calibration_results} summarizes representative results using 100 local calibration samples. Three settings are compared: \textit{Global} denotes the uncalibrated global surrogate, \textit{Local only} denotes a local model fitted directly from current-layout samples and design variables rather than from global-model residuals, and \textit{Calib.} denotes the proposed local calibration of the global surrogate. The \textit{Improv.} column reports the relative change from \textit{Global} to \textit{Calib.} model.

For the feasibility surrogate, the local-only ablation reduces ECE but does not improve the Brier score and leads to a screen recall of only 0.7265, indicating that local samples alone are insufficient for conservative screening. In contrast, calibrating the global feasibility probability decreases the Brier score from 0.0749 to 0.0437 and the expected calibration error from 0.0895 to 0.0333. The screening reject rate increases from 0.4708 to 0.5481, while the screen recall only slightly decreases from 0.9990 to 0.9939, showing that calibration improves filtering efficiency while retaining most feasible candidates.

For the reinforcement surrogate, the local-only ablation already achieves a relatively low MAE of 4.82~t, showing that the mapping from design variables to reinforcement demand can be learned effectively within a fixed layout using a small number of samples. Nevertheless, local residual calibration of the global prediction further improves both accuracy and bias. The MAE decreases from 11.48~t to 2.04~t, the RMSE decreases from 12.30~t to 2.76~t, and the $R^2$ increases from 0.847 to 0.994. These results confirm that the residual calibration model effectively corrects the systematic error of the global reinforcement surrogate in the current layout.

Fig.~\ref{fig:calibration_sample_efficiency} further shows that the calibration benefit appears with a small number of local samples and then gradually saturates. The feasibility-related subplots, Fig.~\ref{fig:calibration_sample_efficiency}(a) and Fig.~\ref{fig:calibration_sample_efficiency}(b), show improved probability calibration and screening capacity, while Fig.~\ref{fig:calibration_sample_efficiency}(c) and Fig.~\ref{fig:calibration_sample_efficiency}(d) show that reinforcement MAE and bias are corrected with limited local data. This is important for optimization, because the local calibration set is accumulated online from candidates that have already been evaluated by FEA.

These results match the intended role of online local calibration in the optimization loop. The global graph model provides the initial cross-layout prediction, and the lightweight local calibrators then adjust its probability and residual estimates using samples verified on the current fixed layout. As a result, the surrogate module can be updated during optimization without retraining the full graph model.

The best feasibility probability calibrator and steel residual calibrator under each calibration size are summarized in Appendix Tables~\ref{tab:appendix_feasibility_calibrator} and~\ref{tab:appendix_steel_calibrator}, respectively.

\subsection{Optimization Efficiency and Solution Quality}
\label{subsec:optimization_results}

The calibrated surrogate models are further embedded into optimization procedures to evaluate their practical effect on FEA efficiency and solution quality. Fig.~\ref{fig:optimization_benchmark_results} and Table~\ref{tab:optimization_summary_results} show that surrogate-assisted evaluation substantially reduces true FEA calls across GA, PSO, and random search. The cost-ranking strategy reduces FEA calls by about 50\%--57\%, while adding feasibility screening increases the reduction to about 62\%--66\%. This consistent reduction across three optimizers indicates that the efficiency gain mainly comes from surrogate-based candidate allocation rather than from a specific search algorithm.

Table~\ref{tab:optimization_summary_results} summarizes the main quantitative results of the optimization experiments. Each optimizer is tested on 15 layouts with five random seeds, resulting in 75 runs for each evaluation strategy. The cost ratio is computed only for paired cases where both the surrogate-assisted strategy and the corresponding full FEA baseline find feasible solutions.


The feasible-solution discovery rate remains close to that of the full FEA baseline. With the Screen \& ranking strategy, the success rate decreases only slightly for GA and PSO and remains nearly unchanged for random search. Although surrogate-assisted methods may miss feasible designs in some cases, the loss in success rate is small compared with the achieved FEA savings.

The final material cost results indicate that the surrogate-assisted strategies do not substantially degrade solution quality, as shown in the second row of Fig.~\ref{fig:optimization_benchmark_results}. For the Screen \& ranking strategy, the average material cost increase relative to the full FEA baseline is about 0\%--2\% among paired cases where both strategies find feasible solutions. The first-feasible distributions further show that screening generally reduces the number of FEA calls required to find the first feasible solution, indicating that feasibility prediction helps the optimizer avoid obviously infeasible regions in the early search stage.

Fig.~\ref{fig:cost_tolerance_analysis} further evaluates the robustness of solution quality in full-feasible cases. The horizontal axis represents the allowed cost tolerance relative to the full FEA optimum, and the vertical axis represents the proportion of cases that simultaneously satisfy three conditions: a feasible solution is found, the number of FEA calls is reduced, and the final cost is within the specified tolerance. As the tolerance increases from 0\% to about 5\%, the qualified-case ratio increases rapidly and then becomes stable. This trend indicates that, for most cases where full FEA optimization can find feasible solutions, the surrogate-assisted strategy can trade a small cost increase for a substantial reduction in FEA calls.


Different optimizers respond differently to surrogate-assisted evaluation. GA benefits from the batch-based candidate generation and population update mechanism, which allows the surrogate module to compare and filter diverse candidates in each generation. PSO tends to concentrate particles earlier in the search process, so the benefit of surrogate screening may depend more strongly on whether enough local calibration samples have been accumulated. Random search does not use historical feedback to guide search directions; therefore, the surrogate module mainly acts as a pre-evaluation filter. The consistent FEA reduction across all three optimizers suggests that the proposed framework improves efficiency primarily by reallocating the evaluation budget from low-value candidates to more promising ones.

\subsection{Mechanism and Practical Implications}
\label{subsec:mechanism_discussion}

This subsection further discusses why this effect occurs and under what conditions the proposed framework is most useful.

Fig.~\ref{fig:candidate_flow_analysis} illustrates the flow of candidates during the surrogate-assisted optimization process. In each generation, candidates can be divided into three groups: candidates rejected by feasibility screening, candidates that pass screening but are skipped by cost-based ranking, and candidates that are finally submitted to true FEA. On average, about 60\% of generated candidates are filtered before FEA. This candidate flow is the direct reason for the reduction in true evaluation calls.


It is worth noting that some candidates submitted to FEA are still infeasible. This is not a failure of the framework, but a consequence of the conservative screening strategy. The feasibility surrogate is designed to avoid rejecting potentially feasible candidates, so it intentionally retains uncertain or boundary candidates for true verification.

Fig.~\ref{fig:optimization_case_examples} provides representative optimization histories for three layouts with different difficulty levels. The representative cases are selected according to the feasible-candidate ratio observed in the optimization histories, with higher ratios indicating easier cases and lower ratios indicating more difficult cases. In the full FEA baseline, all generated candidates are evaluated, including many high-cost candidates and clearly infeasible candidates. When the reinforcement surrogate is used, evaluations are more concentrated in the low-cost region. However, cost-based ranking alone may still select low-cost but infeasible candidates. After feasibility screening is added, fewer infeasible candidates enter FEA, and the evaluated candidates are more concentrated around promising feasible regions. The best feasible costs remain close to those obtained by full FEA optimization in most cases.


The two surrogate tasks therefore play complementary roles. The reinforcement surrogate reduces evaluations of obviously high-cost candidates and guides the search toward economical designs. The feasibility surrogate reduces evaluations of low-cost but clearly infeasible candidates. Online local calibration further adapts both predictions to the current layout, reducing the influence of global surrogate bias on candidate selection.

For the difficult case, feasible designs are not located in the lowest-cost region; instead, the search needs to move toward larger sections and higher material costs to reach the feasible domain. This trend is observed in the full FEA baseline, where evaluated candidates gradually shift to higher-cost regions and feasible solutions eventually appear. In contrast, the cost-surrogate strategy overemphasizes low-cost candidates, which are mostly infeasible for this layout, and therefore fails to provide useful feedback for moving the search toward feasibility. After feasibility screening is introduced, the screen \& cost ranking strategy filters out low-cost but low-feasibility candidates and retains candidates with higher passing potential. As a result, the search direction becomes closer to the full FEA baseline, enabling feasible solutions to be found with fewer true evaluations.


Fig.~\ref{fig:layout_difficulty_heatmap} shows that layout difficulty is a key factor in feasible-solution discovery. Layouts that remain difficult even under full FEA optimization are likely limited by the layout itself or by the section-material design space, rather than by surrogate screening. Therefore, the proposed method is most useful when feasible candidates exist but FEA evaluations are expensive and limited: in such cases, calibrated surrogate assistance can reduce unnecessary evaluations with limited impact on final design quality.
