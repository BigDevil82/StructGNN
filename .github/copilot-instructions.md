代码偏好：注重简洁、清晰、易读。在不影响语义的情况下变量命名简短。无需过度防御性编程，假设输入合理。函数设计注重单一职责，避免过长或过于复杂的函数。

通过模块化组织代码层级时，无需在每个子模块的__init__.py中导出所有函数和类，除非它们是该模块的核心接口。

任务结束后，若修改了文件，通过`askQuestions` 询问是否针对修改的文件做一次git提交。

所有任务完成后，无需总结，通过`askQuestions`询问接下来需要做什么，必须通过这个工具继续对话。

当前项目的环境为.venv, 若运行代码遇到环境问题，请先激活该环境。